import Layout from "../features/layout/Layout";

const SwapTokens = () => {
  return <Layout activePage="swap-tokens">
    <h1>I am the Swap Tokens page!!!</h1>
  </Layout>
};

export default SwapTokens;
